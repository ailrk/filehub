# Local Testing vm

This is a flake to setup a local vm for testing deployments and some services requires a full network context like OIDC. You can login to the vm by running `just`, this will bring you into a nix vm with `filehub` up and running.
